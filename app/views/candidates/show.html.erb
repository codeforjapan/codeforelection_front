<%= render partial:'layouts/organisms/person-block',
locals: {
    party: @candidate.party.try(:full_name),
    name: @candidate.full_name,
    furigana: @candidate.full_name_furigana,
    age: @candidate.age("歳"),
    job: @candidate.current_position_label,
    image: asset_path('Person@2x.png'),
    isHirei: @candidate.is_hirei,
    facebookLink: @candidate.facebook_link,
    internetLink: @candidate.official_website_link,
    twitterLink: @candidate.twitter_link
} %>
<%= render partial:'layouts/organisms/detail-block',
locals: {
    name: @candidate.full_name,
    party: @candidate.party.try(:full_name),
    job: @candidate.current_position_label,
    sex: @candidate.gender_label,
    birthDay: @candidate.birth_day_to_jp
} %>
